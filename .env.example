# Environment template (do not commit secrets)
APP_ENV=local
LOG_LEVEL=info
GUICE_SCAN_CLASSPATH=true
GUICE_ALLOW_PATHS=true
# Add host-specific overrides as needed; keep secrets in vaults/CI secrets, not in this file.
